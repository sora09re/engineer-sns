export type User = {
  id: number;
  bio: string;
  createdAt: Date;
  email: string;
  followerCount: number;
  followingCount: number;
  passwordHash: string;
  profileImageUrl: string;
  updatedAt: Date;
  username: string;
};
